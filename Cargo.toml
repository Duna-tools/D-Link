[package]
name = "d-link"
version = "0.1.0"
description = "High-performance TI-Nspire terminal management system"
authors = ["Lucas Boniche"]
edition = "2021"

[dependencies]
# Ratatui ecosystem
ratatui = "0.28.1"
crossterm = "0.28.1"
tui-textarea = "0.6.0"

# USB & System
rusb = "0.6" # Mantenemos 0.6 por compatibilidad con libnspire
libnspire = "0.2"
dirs = "5.0"
lazy_static = "1.4"
hashbrown = "0.14"

# Async & Utilities
tokio = { version = "1.36", features = ["full"] }
anyhow = "1.0"
thiserror = "2.0"
chrono = "0.4"
crossbeam-channel = "0.5"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

# Logging
log = "0.4"
image = { version = "0.25.9", features = ["png"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
